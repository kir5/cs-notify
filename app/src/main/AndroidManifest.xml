<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.csnotify.k5.csnotify">

    <uses-permission android:name="android.permission.INTERNET"></uses-permission>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!--
        activity android:name=".LoginTab1Fragment" />
        <activity android:name=".LoginTab2Fragment" /
        -->
        <activity android:name=".HomeActivity" />
        <activity android:name=".ChatRooms" />
        <activity android:name=".NavigationHeaderActivity" />
        <activity android:name=".HelpActivity" />
        <activity android:name=".MyAccountActivity" />
        <activity android:name=".ChangePasswordActivity" />
        <activity android:name=".BackgroundActivities.LoadingActivity"></activity>
    </application>

</manifest>